# Diabetics-Prediction-
Diabetes Prediction using Machine Learning predicts whether a person is diabetic based on health parameters like glucose, BMI, and age. It includes data preprocessing, EDA, and models like Logistic Regression and Random Forest, evaluated using accuracy and confusion matrix for reliable results.
